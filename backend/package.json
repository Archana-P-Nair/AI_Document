{
  "name": "ai-document-backend",
  "version": "1.0.0",
  "description": "Backend for AI Document Platform",
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000"
  }
}
